services:
  - path: services/example
    name: example
    # Inline SDK config until FluffyLabs/jammin#123 ships in a release.
    # Once available, replace with: sdk: jamc3-<new>
    sdk:
      image: ghcr.io/dreverr/jamc3:2.0.2
      build: main.c3 -o service.jam
      test: "--help"
